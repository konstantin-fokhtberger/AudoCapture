# Контролируемый pilot: B09/B11/B14

Статус: **начат, приёмка не выполнена**. Первый запуск после B10, 2026-09-08. Автоматические тесты и малый размер bundle не заменяют эти испытания.

## Первый запуск: промежуточный результат

- Release из baseline `08d2001`, текущий Mac; пользователь разрешил использовать открытый YouTube и тестовые фразы в микрофон. Воспроизведение ролика агентом пока не запускалось.
- Системный звук не стартовал: приложение сообщает отказ Screen Recording, хотя переключатель AudoCaptureApp в настройках включён. Перезапуск release не устранил расхождение.
- У старого bundle и текущего release разные ad-hoc designated requirements (cdhash). Несоответствие сохранённого разрешения текущей сборке - гипотеза, требует проверки добавлением точного release bundle.
- При старте с микрофоном `.Sony` наблюдались отмена из-за изменения аудиоустройств и остановка из-за изменения конфигурации микрофона. Штатное переключение Bluetooth-профиля как причина пока не доказано.
- UI показал два коротких частичных результата `08-09-2026 11-25.m4a` и `08-09-2026 11-25 (2).m4a`. Их содержимое ещё не проверено; это не подтверждение записи обоих источников.
- Запись остановлена. Добавление текущей сборки через системные настройки ожидает пользовательского Touch ID/пароля. После этого: встроенный микрофон + YouTube, проверка единого M4A; Bluetooth проверять отдельно.

## Перед запуском
- Зафиксировать commit, debug/release, модель Mac, версию OS, выбранные вход/выход и доступные permissions.
- Использовать отдельную тестовую сессию с известным звуком и тестовой речью; перед живой записью согласовать её начало с пользователем.
- Итоговые аудиозаписи остаются локально, вне Git. Автоматической отправки в транскрибатор нет.

## Порядок
| № | Сценарий | Что проверяем | Статус |
|---|---|---|---|
| 1 | Короткая запись, встроенный микрофон + системный тестовый звук | Оба источника в одном M4A, начальные паузы и одновременный звук; дата/время Start, декодирование, длительность | todo |
| 2 | 20 Start/Stop, несколько запусков в одну минуту | Независимые файлы, суффиксы без перезаписи, освобождение устройств, нет накопления ресурсов | todo |
| 3 | USB/Bluetooth, отключение и смена default input/output во время записи | Видимый Stop/partial с объяснением, доступное аудио сохранено; нет скрытого продолжения с другим источником | todo |
| 4 | Изменение аудиоформата/дисплея, сон/wake | Нет ложного recording; состояние и metadata честные; сохранение после wake при необходимости, без auto-restart | todo |
| 5 | Quit во время capture и экспорта | Приложение ждёт завершения, M4A воспроизводится и содержит доступный хвост | todo |
| 6 | 30 min, затем 2 h с известными звуковыми отметками обоих источников | Сохранность порядка, пауз и относительных смещений в начале/середине/конце; нет исчезновения дорожки | todo |
| 7 | Измерение лёгкости на сценарии 6 | Idle/record/export CPU, RSS и peak RAM, временный диск, размер M4A; замеры по времени, проверка устойчивого роста памяти | todo |
| 8 | Ошибка записи/экспорта и Retry | Ошибка видна, исходники остаются; после устранения причины доступен результат без recapture. Автотесты уже покрывают управляемые ошибки, реальный disk-error сценарий отдельно | todo |
| 9 | Импорт в выбранный пользователем транскрибатор | M4A принимается, обе стороны распознаются; до выбора сервиса статус unknown | unknown |

## Фиксация результатов
Для каждого прогона записать факт, измерения, отклонения и ссылку на локальный артефакт. Отдельно отметить, где используется субъективное прослушивание и где измерение известного сигнала. Не объявлять B09/B11/B14 закрытыми по одному короткому успешному файлу.

Поддержка другого Mac/macOS 14/15/26 и установка скачанного подписанного дистрибутива относятся к B12/B13. Нынешний ad-hoc release не считается таким дистрибутивом.

## Диагностика разрешения после повторного добавления bundle

- Добавление точного release bundle и перезапуск не устранили отказ.
- TCC, 2026-09-08 11:43:41-42: `Failed to match existing code requirement for subject dev.codex.AudoCapture and service kTCCServiceScreenCapture`. Причина подтверждена: сохранённое code requirement не соответствует текущей сборке.
- Выполнен адресный `tccutil reset ScreenCapture dev.codex.AudoCapture`, команда подтвердила успешный сброс. Другие разрешения не сбрасывались.
- Текущая сборка повторно запрошена; переключатель ScreenCapture выключен, его включение ожидает Touch ID пользователя. Успешный доступ и live capture ещё не подтверждены.
- Для повторяемых сборок требуется стабильная signing identity (проверить в B12); ad-hoc подпись меняется вместе с бинарником. Не ослаблять code requirements для обхода TCC.

## Короткий live-прогон после восстановления TCC

- 2026-09-08 11:46:59, release `08d2001`: встроенный микрофон MacBook Air + YouTube. Разрешение применилось. Первый Start отменён уведомлением об изменении устройств; повторный успешен. Причину проверить в B09.
- Сессия `8E71AEC1-C737-40A7-9E34-79E49A0350BF`: capture/postProcessing completed, errors/warnings пустые. Один `08-09-2026 11-46.m4a`, 48.177833 s, AAC stereo 48 kHz. Полное декодирование через afconvert прошло вне sandbox (в sandbox codec error fmt?).
- Mic: 2311646 frames; system: 2311655 frames, оба 48 kHz. Разница длительностей 0.1875 ms не доказывает синхронизацию содержимого. Прослушивание обоих источников пользователем ещё не подтверждено.
- Разовый снимок release: CPU 5.7%, RSS 93744 KiB. Это не профиль нагрузки; одновременно обнаружен старый idle bundle.
- Запись остановлена, ролик поставлен на паузу. Аудио локально, вне Git. Длительные тесты и Bluetooth не закрыты.

## Проверка новой сборки и стабильной подписи

- После изменения бинарника TCC отверг обе старые ad-hoc permissions (Microphone/ScreenCapture). На Mac найден один валидный Apple Development сертификат; сборщик теперь выбирает единственный доступный сертификат или явный AUDOCAPTURE_SIGNING_IDENTITY, при отсутствии предупреждает об ad-hoc.
- Release собран, подписан Apple Development и запущен. Designated requirement основан на сертификате и bundle ID вместо cdhash. Адресно сброшены оба разрешения AudoCapture, после подтверждения пользователем оба доступны. Сохранение разрешений после следующего изменения бинарника ещё проверить.
- Первый Start после перезапуска сразу запустил оба источника. Затем получено уведомление об изменении дисплеев: автоматический Stop и partial M4A `08-09-2026 12-00.m4a`. Это не успешный непрерывный pilot. Следующая диагностика B09: сравнить реальную конфигурацию дисплеев при уведомлениях; не считать само уведомление доказательством потери источника.
- Запись остановлена. Проверка codesign в sandbox дала CSSMERR_TP_NOT_TRUSTED; проверять доверие вне sandbox, где выполнялись сборка и подпись.
