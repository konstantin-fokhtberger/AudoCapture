# Бэклог AudoCapture: простой рабочий выпуск

- Дата: 2026-09-08.
- Основание: пользователь поручил зафиксировать задачи и начать итерационную реализацию, отдавая приоритет простоте и быстрому использованию.
- Это текущий рабочий бэклог. Он сокращает объём первого выпуска по сравнению с полным планом аудита, не отменяя выявленные риски.
- Статусы: `todo`, `in_progress`, `done`, `deferred`. Done означает выполнение указанного критерия, а не готовность всего приложения.
- Правило: одна ограниченная итерация -> проверка -> краткий результат -> следующая итерация. Без нового backend, DI container, DB, сервера, виртуального драйвера или двух реализаций захвата для разных OS.

## Цель первого выпуска

Открыть приложение, нажать Record, затем Stop, получить один автоматически сохранённый локальный M4A. Существующие ScreenCaptureKit + AVAudioEngine, нативный UI, Apple Silicon. Целевая матрица macOS 14/15/26; заявлять поддержку только после проверки.

Подтверждено пользователем: итоговый файл должен быть один - M4A/AAC с именем по локальной дате и времени старта, например `08-09-2026 10-04.m4a`, содержащий микрофон и весь системный звук. Раздельные дорожки - только промежуточные файлы; они остаются резервом при ошибке экспорта. После проверки общего файла промежуточные файлы удаляются. Сведение и необходимое выравнивание входят в первый выпуск; сложная адаптивная коррекция drift не вводится без измеренной необходимости.

Для первого выпуска при смене устройства/формата или сне допустимо явно остановить запись и безопасно сохранить доступное. Бесшовное продолжение не требуется. При недоступности источника остаётся существующий честный single-track fallback с заметным предупреждением. Незаметная потеря дорожки недопустима.

## Итерации

| Итерация | Результат | Задачи | Статус |
|---|---|---|---|
| I1 | Поток не обрывается после первого преобразованного буфера | B01 | done |
| I2 | Безопасные Start/Stop и сохранение | B02-B05 | done |
| I3 | Один M4A без сторонних программ; код и синтетические проверки | B06-B07, B14 (live часть в I5) | done |
| I4 | Удобный запуск и обработчики прерываний; hardware-проверки в I5 | B08-B09 | done |
| I5 | Рабочая локальная версия и короткий pilot | B10-B11 | in_progress |
| I6 | Передача коллегам | B12-B13 | todo |

Не накапливать косметические изменения между проверками. Первая реальная запись - после I2 на текущем Mac; это промежуточная диагностика, а не приёмка экспорта или разрешение на распространение. После I5 можно пользоваться локально с явно записанными ограничениями.

## Обязательный бэклог

| ID | Работа | Критерий завершения | Зависимости | Статус |
|---|---|---|---|---|
| B01 | Исправить `.endOfStream` между порциями; regression tests mono/stereo, разные sample rate | Реальная функция обрабатывает последовательности буферов без пропадания звука; новые тесты воспроизводят старый дефект и проходят после исправления | - | done |
| B02 | Простые guards состояния в manager; уникальный ID/каталог сессии; single-use либо reset writer | Двойной Start отклонён, Start во время Stop запрещён, существующие файлы не перезаписываются/не удаляются, метрики writer корректны | B01 | done |
| B03 | Корректное завершение capture: drain converter, очередь callbacks, finalize во всех ветках, безопасный Quit | Хвост аудио сохранён; обработка дольше 5 s не приводит к преждевременному выходу; stop failure не запускает экспорт незакрытого файла | B02 | done |
| B04 | Один простой канал ошибки source -> manager -> UI; capture/export status раздельно; unknown metrics | Ошибка после Start заметна, сохранённый неполный результат не обозначается полным; отсутствующая дорожка не считается drift; прекращается бесконечное логирование одной ошибки | B02-B03 | done |
| B05 | Честный lifecycle разрешений и отмены | Нет фиктивного 3 s timeout, операция старта имеет контролируемый результат отмены; настройки доступны при degraded startup; тесты denied/granted/retry | B02 | done |
| B06 | Нативный AAC/M4A encoder; убрать subprocess MP3 и PATH-зависимость | Один итоговый M4A с датой-временем старта в имени создаётся без ffmpeg/lame; проверены декодирование, каналы, длительность, ошибки и отмена экспорта; обе стороны слышны | B03-B04, B14 | done |
| B07 | Простой надёжный экспорт файлов | Имя `dd-MM-yyyy HH-mm.m4a` из неизменного startedAt (после permissions, при старте capture), локальный часовой пояс на старте; UI показывает `HH:mm`; суффикс `(2)` при коллизии без перезаписи. Уникальная папка, временное имя и финальная публикация после проверки; при ошибке исходники остаются; удаление промежуточных дорожек только после успешной проверки общего M4A; retry экспорта без повторной записи | B06 | done |
| B08 | Минимальный UI: Record/Stop, таймер, активные источники, результат, открыть папку | Запуск понятен без Refresh-ритуалов; выбор микрофона блокируется при записи; completed/partial/failed различимы; ошибки содержат действие | B04-B07 | done |
| B09 | Реакция на отключение/смену устройства, формата, дисплея и сон | Контролируемый Stop с сохранением и объяснением вместо ложного recording; встроенный и Bluetooth сценарии проверены; без автоматического reconnect | B03-B04 | in_progress |
| B10 | Минимальная гигиена и воспроизводимая сборка | `.gitignore`, baseline исходников в Git, согласованные OS/version/README; debug/release bundle; no-op/temp logger и cleanup tests; детерминированный тест конкурентности; проверить избыточную аллокацию device catalog | I1-I4 | done |
| B11 | Pilot и проверка лёгкости | На текущем Mac: обе стороны слышны, 30 min и 2 h, 20 циклов Start/Stop, гарнитура, Quit, disk/write errors; измерены CPU/RAM и размер; память не растёт со временем; выбранный транскрибатор читает M4A либо совместимость честно unknown | B08-B10 | todo |
| B12 | Подпись и упаковка arm64 | Developer ID, hardened runtime, notarization, stapling, ZIP; установка скачанного артефакта на другом Mac со стандартным Gatekeeper, без developer tools | B11, доступ к Apple Developer Program | todo |
| B14 | Простое сведение двух источников в один файл | Общая временная шкала/начальное смещение и сохранение пауз; уровни не дают clipping; микрофон и система слышны в одном M4A, включая одновременную речь; синтетические сигналы и длительная запись проверены | B03-B04 | in_progress |
| B13 | Совместимость и выпуск для коллег | Проверены macOS 14/15/26 на доступных M-series Mac; версия и краткая инструкция; pilot обратная связь; непроверенную OS не объявлять поддержанной | B12, доступ к целевым Mac | todo |

Профилирование B11 служит проверкой требования «легковесное». Не вводить обязательные сложные ring buffers или custom executors заранее: если bounded обычная очередь требуется для устранения потерь/нагрузки, включить минимальный вариант в B09/B11, до приёмки. Синхронный I/O в callback остаётся явным риском до этой проверки.

## Результат итерации I1

B01 выполнена: между порциями converter получает `.noDataNow`, а не `.endOfStream`. Добавлены реальные regression tests: 20 mono-буферов на каждой из 16/44.1/48/96 kHz и stereo-последовательность с противоположными сигналами в каналах. До исправления тесты воспроизвели дефект (157 assertions); после - 22 теста в 6 suites прошли. Завершение и drain converter остаются B03; реальная запись ещё не проверена. Следующая после I1 итерация B02-B05 завершена; см. результат I2 ниже.

## Результат итерации I2

B02-B05 реализованы и проверены: guards manager, уникальные папки, reset writer, drain/close, ожидание Quit, ошибки source -> UI, partial/unknown metadata и отмена permissions. 38 тестов в 9 suites и smoke check прошли. Статус done здесь означает код и автоматические проверки; реальные privacy dialogs, аудиоустройства и compatibility остаются B09/B11-B13. См. [отчёт I2](../05-implementation/2026-09-08_iteration-02-safe-recording.md).

## Результат итерации I3

B06-B07 реализованы: нативный AAC/M4A, единый датированный файл, проверка декодированием, эксклюзивная публикация, сохранение исходников при ошибке и Retry Saving. Внешний MP3 exporter удалён. B14 реализована и проверена синтетически; остаётся in_progress до длительной реальной записи B11. Закрытие кода I3 не означает аппаратную приёмку приложения.

Автоматические проверки: 46 тестов в 10 suites и smoke check прошли; см. [итоговый отчёт I3](../05-implementation/2026-09-08_iteration-03-single-m4a.md). Реализация I4 завершена, см. ниже.

## Результат итерации I4

B08 выполнена: компактный UI, одна Start/Stop-кнопка, timer, active tracks, refresh при activation/wake, Finder. B09 реализована в коде (HAL/OS/engine notifications, cancellation/Stop, причина interruption в metadata), но остаётся in_progress до физических испытаний USB/Bluetooth, дисплеев и сна. 52 теста в 11 suites прошли; debug bundle запущен, idle-окно проверено визуально без аудиозаписи. См. [отчёт I4](../05-implementation/2026-09-08_iteration-04-ui-and-interruptions.md).

Run action и тонкий build/run wrapper уже добавлены как часть удобного запуска B10; прочие критерии B10 остаются открытыми. Следующая итерация: B10, затем контролируемая живая проверка B09/B11/B14. Завершение экспорта до принудительного сна не гарантируется.

## I5: результат B10

Реализованы ignore, arm64/macOS 14, отдельные debug/release bundle, test logger/cleanup, startup barrier и исправление аллокации AudioBufferList. 54 теста в 12 suites прошли, оба bundle собраны и проверены. По запросу пользователя создан закрытый GitHub AudoCapture; baseline `2d8605d` отправлен в main; приватность и совпадение SHA проверены. Release пересобран из чистого Git checkout, B10 закрыта. См. [отчёт B10](../05-implementation/2026-09-08_iteration-05-build-baseline.md).

Следующий шаг: [контролируемый pilot](../06-testing/2026-09-08_pilot-checklist.md), B09/B11/B14. I5 остаётся in_progress до реальных испытаний.

## Отложенные задачи

| ID | Работа | Когда возвращаемся | Статус |
|---|---|---|---|
| D02 | Адаптивная коррекция drift и точная синхронизация | Если минимальная временная шкала B14 не проходит измерения; общий файл обязан сохранять порядок и время речи | deferred |
| D03 | Сегментация, crash recovery и периодические checkpoints | Если нужен гарантированный recovery/очень долгие записи; первый выпуск не обещает сохранность после force quit/power loss | deferred |
| D04 | Бесшовное переключение/переподключение устройств и продолжение после сна | По реальной потребности; сначала безопасная остановка | deferred |
| D05 | AEC/подавление акустического дубля | Если использование динамиков требует этого; для начала рекомендуемый сценарий с наушниками | deferred |
| D06 | Menu bar, hotkey, автозапуск, автообновления, настройки формата | После подтверждения удобства базового сценария | deferred |
| D07 | Встроенная транскрибация, diarization, облачная интеграция | Только по отдельному запросу | deferred |
| D08 | Альтернативный backend Core Audio taps / SCK microphone 15+ | Только если существующий capture не проходит целевые сценарии | deferred |
| D09 | Косметический рефакторинг public API/naming, тонкий DI, дополнительные метрики | По мере изменения соответствующего кода; не задерживать первый запуск | deferred |
| D10 | Дистрибутив DMG вместо ZIP, дополнительные ресурсы оформления | Если это улучшит установку после pilot | deferred |

Внешний MP3 encoder удалён в B06; прежние проблемы subprocess pipes/SIGTERM устранены удалением этого пути.

## Покрытие всех находок двух ревью

| Источник | Задачи |
|---|---|
| A01 / CQ-04 | B04 |
| A02 / CQ-03 | B03 |
| A03 | B14 (выравнивание/сведение), B11 (замер); D02 только для дополнительной коррекции |
| A04 / CQ-01 | B01, B03 (tail) |
| A05 / CQ-08 | B04 (ограничение error path), B11 и при необходимости bounded writer |
| A06 | B09; D04 для бесшовного продолжения |
| A07-A08 / CQ-06 | B05-B06 |
| A09 / CQ-05 | B02 |
| A10 / CQ-07 | B03/B07; D03 для crash recovery |
| A11 / CQ-10 | B04/B06-B07 |
| A12 | B12-B13 |
| A13 | B05/B08 |
| A14 / CQ-11-CQ-12 | B10; B04 для ограниченного логирования |
| Дополнительное замечание manager / CQ-02 | B02 |
| CQ-09 | B02 |
| Прочий долг code quality review | B10 (аллокации/config), D09; ownership capture проверяется в B03/B09 |

D01 объединена с обязательной B14 по уточнению пользователя: общий файл входит в первый выпуск.

## Неизвестные зависимости

- Доступность Developer ID/Apple Developer Program и Mac коллег с целевыми OS. Выяснить перед B12, не блокирует I1-I5.
- Целевой сервис транскрибации и его лимиты. Для первого выпуска не добавлять сервис, аккаунты или API; проверить импорт, когда сервис выбран.
- Согласие на live recording и privacy prompts определяется текущим явным запросом/контекстом перед испытанием; не записывать посторонние разговоры в фоне.

## Источники

- [Продуктовый аудит](../07-reports/2026-09-08_audit-and-work-plan.md).
- [Качество кода](../07-reports/2026-09-08_code-quality-review.md).
- [Доказательства code quality](../07-reports/2026-09-08_code-quality-evidence.md).
- Текущая инструкция пользователя: быстрый запуск, простое легковесное приложение, итерационная реализация. Дополнительное явное уточнение: итоговый файл должен быть один.
- Исправление рабочего предложения: вариант двух итоговых M4A отклонён пользователем до реализации. B14 включена в первый выпуск.

- Дополнительное требование пользователя: имя итогового файла содержит дату и время старта, пример отображения `08-09-2026 10:04`; переносимое имя файла `08-09-2026 10-04.m4a`.

## Уточнение после live pilot

Пользователь подтвердил оба источника на слух и решил оставить текущий баланс без усиления микрофона. Семинарный тест 11:51: один M4A 29.165 s, completed, ошибок нет; границы речевых участков не подтверждены. Нормализацию/AGC не добавлять.

B09: обработчик HAL сравнивает список устройств и default input/output. Повторное уведомление и добавление устройства при неизменном маршруте только обновляют список; удаление или смена маршрута, а также ошибка чтения сохраняют остановку. 55 тестов в 12 suites прошли. Это устранение излишне широкого условия остановки; связь с наблюдавшимся первым Start ещё требует live-проверки новой сборки.

После пересборки ad-hoc bundle возможно повторное несовпадение TCC. Перед следующим pilot проверить разрешение текущей сборки; при подтверждённом mismatch сбросить только ScreenCapture dev.codex.AudoCapture и выдать заново. Стабильная подпись остаётся B12.

## Дополнения: дисплеи, Bluetooth, Finder

- По просьбе пользователя Stop и Retry больше не открывают Finder автоматически; ручная кнопка остаётся.
- Дисплейные уведомления сравнивают main display ID и набор подключённых экранов; неизменная конфигурация только обновляет UI. Тест проверяет отсутствие остановки на повторном уведомлении и остановку при изменении.
- Bluetooth B09 остаётся открытым: пользователь выбирает Sony до Start, после старта AVAudioEngineConfigurationChange останавливает запись. Воспроизведено агентом. Игнорирование уведомления только при работающем движке и совпадающем формате не устранило сбой. Эксперименты с 300 ms стабилизацией и отключением output IO не помогли и удалены. Добавлена диагностика running/expected/actual format/selected device для следующего прогона. Не объявлять Bluetooth исправленным.
- Разрешения после повторных пересборок с Apple Development сохранились, новый TCC reset не потребовался.

## Bluetooth: успешная короткая проверка

Микрофон переведён на AVCaptureSession с явным UID. Sony + system: 51.6 s без обрыва, единый M4A успешно декодирован. Встроенный микрофон после Sony также прошёл короткую проверку. См. [отчёт](../05-implementation/2026-09-08_bluetooth-capture-session.md). B09 остаётся in_progress до disconnect/длительных проверок; прежнее описание AVAudioEngine относится к старой реализации.

## UI: компактный и полный режимы

По запросу пользователя добавлена компактная панель 200 x 56 pt (200 x 88 со стандартной строкой заголовка на текущей OS): Record/Stop, On Air/состояние, разворачивание. Полный режим сохраняет настройки. Выбор режима запоминается. Ошибки и single-track отмечаются предупреждением в компактном индикаторе.

Release собран; визуально проверены оба размера, сворачивание во время реальной записи, Stop из компактного вида (13 s, UI Сохранено), восстановление режима после запуска. По уточнению пользователя Play заменён на Record.

## Build 3: независимый mute и компактное окно поверх

Микрофон/системный звук исключаются из записи кнопками в обоих видах, до Start и во время записи. Writer записывает тишину с сохранением количества кадров; переключение завершает конвертер под предыдущим состоянием. Оба mute дают тишину, capture остаётся активным. Аппаратный микрофон остаётся открыт, звук в наушниках не отключается. При перезапуске оба источника включены.

Компактное окно использует NSWindow.Level.floating и доступно на Spaces, полный вид - normal. UI проверен в обоих видах. 57 тестов прошли, включая mute до Start, независимое переключение в manager, mute/unmute с конвертацией 16/48 kHz. Реальный M4A 13:25: 17.0969 s, completed, peak PCM=0 при обоих mute. Build 3 для дневного использования.


## Build 4: папка сохранения и иконка

- Выполнено: выбор папки в полном окне через NSOpenPanel, запоминание в UserDefaults, применение к новой сессии. При недоступной пользовательской папке нет молчаливого fallback. Текущие/старые записи не перемещаются.
- Выполнено: иконка микрофона, ICNS включён в bundle; воспроизводимый генератор в Scripts.
- Проверено: 59 тестов, release arm64, подпись Apple Development, запуск приложения, выбор папки и перезапуск. Другой путь и недоступная папка проверены автоматическими тестами.
- B12 остаётся открытой: Xcode показывает только Personal Team для подключённого аккаунта; в Keychain есть Apple Development, Developer ID Application не найден. Notarization не выполнена. ZIP build 4 - pilot для ручного допуска запуска, установка на Mac коллег ещё не проверена.
